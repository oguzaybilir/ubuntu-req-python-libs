**Bu repository temel görüntü işleme ve yapay zeka ile bilgisayarlı görü projeleri için gerekli olan python kütüphanelerini içermektedir.**

```bash
  Kullanımı şu şekildedir ; 
  pip3 install -r requirements.txt
```
